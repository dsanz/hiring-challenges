package com.example.host.csp;

/**
 * A Content Security Policy.
 *
 * <p>
 * TODO (T1): a policy is not a string. Model it here, and give it a
 * serialization that is canonical &mdash; same inputs, same bytes, every time.
 * </p>
 */
public class CspPolicy {
}
